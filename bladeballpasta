--[[
    Blade Ball Mobile Hub
    Рабочий Auto Parry + мобильное меню
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Stats = game:GetService("Stats")
local LocalPlayer = Players.LocalPlayer

-- ═══════════════════════════════════════
-- Конфиг (управляется через GUI)
-- ═══════════════════════════════════════
local Config = {
    Enabled = true,
    EmergencyRadius = 10,
    DistanceToParry = 0.65,
    ClashDistance = 22,
    ClashSpeed = 0.01,
    NormalSpeed = 0.05,
    PingBased = true,
    PingBasedOffset = 1.8,
    BaseWindow = 0.45,
    SpeedWindowFactor = 0.002,
    AccelWindowFactor = 0.0001,
    MaxWindow = 0.7,
    MinWindow = 0.25,
    BallESP = true,
    ShowESP = false
}

local IsParrying = false
local LastClick = 0
local LastBallHit = nil
local velocitySamples = {}
local lastSampleTime = 0

-- ═══════════════════════════════════════
-- МОБИЛЬНЫЙ GUI
-- ═══════════════════════════════════════
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BladeBallHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Основной фрейм
local MainFrame = Instance.new("Frame")
MainFrame.Name = "Main"
MainFrame.Size = UDim2.new(0, 260, 0, 370)
MainFrame.Position = UDim2.new(0, 15, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local Stroke = Instance.new("UIStroke")
Stroke.Color = Color3.fromRGB(90, 45, 180)
Stroke.Thickness = 1.5
Stroke.Parent = MainFrame

-- Заголовок
local Header = Instance.new("Frame")
Header.Size = UDim2.new(1, 0, 0, 36)
Header.BackgroundColor3 = Color3.fromRGB(90, 45, 180)
Header.BorderSizePixel = 0
Header.Parent = MainFrame
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 10)

-- Заплатка чтобы нижние углы заголовка были прямые
local HeaderFix = Instance.new("Frame")
HeaderFix.Size = UDim2.new(1, 0, 0, 12)
HeaderFix.Position = UDim2.new(0, 0, 1, -12)
HeaderFix.BackgroundColor3 = Color3.fromRGB(90, 45, 180)
HeaderFix.BorderSizePixel = 0
HeaderFix.Parent = Header

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -40, 1, 0)
Title.Position = UDim2.new(0, 12, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "⚔️ Blade Ball Hub"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextSize = 15
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Header

-- Кнопка свернуть
local MinBtn = Instance.new("TextButton")
MinBtn.Size = UDim2.new(0, 28, 0, 28)
MinBtn.Position = UDim2.new(1, -32, 0, 4)
MinBtn.BackgroundColor3 = Color3.fromRGB(60, 30, 130)
MinBtn.BorderSizePixel = 0
MinBtn.Text = "—"
MinBtn.TextColor3 = Color3.new(1, 1, 1)
MinBtn.TextSize = 16
MinBtn.Font = Enum.Font.GothamBold
MinBtn.Parent = Header
Instance.new("UICorner", MinBtn).CornerRadius = UDim.new(0, 6)

-- Контейнер
local Container = Instance.new("ScrollingFrame")
Container.Size = UDim2.new(1, -16, 1, -44)
Container.Position = UDim2.new(0, 8, 0, 40)
Container.BackgroundTransparency = 1
Container.ScrollBarThickness = 2
Container.ScrollBarImageColor3 = Color3.fromRGB(90, 45, 180)
Container.CanvasSize = UDim2.new(0, 0, 0, 0)
Container.AutomaticCanvasSize = Enum.AutomaticSize.Y
Container.Parent = MainFrame

local Layout = Instance.new("UIListLayout")
Layout.Padding = UDim.new(0, 6)
Layout.SortOrder = Enum.SortOrder.LayoutOrder
Layout.Parent = Container

-- Сворачивание
local isMinimized = false
MinBtn.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    if isMinimized then
        MainFrame.Size = UDim2.new(0, 260, 0, 36)
        Container.Visible = false
        MinBtn.Text = "+"
    else
        MainFrame.Size = UDim2.new(0, 260, 0, 370)
        Container.Visible = true
        MinBtn.Text = "—"
    end
end)

-- ═══════════════════════════════════════
-- UI Компоненты
-- ═══════════════════════════════════════
local function AddSeparator(text)
    local sep = Instance.new("TextLabel")
    sep.Size = UDim2.new(1, 0, 0, 22)
    sep.BackgroundTransparency = 1
    sep.Text = "— " .. text .. " —"
    sep.TextColor3 = Color3.fromRGB(130, 100, 200)
    sep.TextSize = 11
    sep.Font = Enum.Font.GothamBold
    sep.Parent = Container
end

local function AddToggle(name, default, callback)
    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(1, -4, 0, 34)
    Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 40)
    Frame.BorderSizePixel = 0
    Frame.Parent = Container
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 7)

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(1, -60, 1, 0)
    Label.Position = UDim2.new(0, 10, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Text = name
    Label.TextColor3 = Color3.fromRGB(220, 220, 230)
    Label.TextSize = 13
    Label.Font = Enum.Font.Gotham
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Frame

    local Btn = Instance.new("TextButton")
    Btn.Size = UDim2.new(0, 44, 0, 22)
    Btn.Position = UDim2.new(1, -52, 0.5, -11)
    Btn.BorderSizePixel = 0
    Btn.Text = ""
    Btn.Parent = Frame
    Instance.new("UICorner", Btn).CornerRadius = UDim.new(1, 0)

    local Dot = Instance.new("Frame")
    Dot.Size = UDim2.new(0, 16, 0, 16)
    Dot.BorderSizePixel = 0
    Dot.BackgroundColor3 = Color3.new(1, 1, 1)
    Dot.Parent = Btn
    Instance.new("UICorner", Dot).CornerRadius = UDim.new(1, 0)

    local enabled = default
    local function Refresh()
        local ti = TweenInfo.new(0.15, Enum.EasingStyle.Quad)
        if enabled then
            Btn.BackgroundColor3 = Color3.fromRGB(80, 200, 80)
            TweenService:Create(Dot, ti, {Position = UDim2.new(1, -19, 0.5, -8)}):Play()
        else
            Btn.BackgroundColor3 = Color3.fromRGB(70, 70, 80)
            TweenService:Create(Dot, ti, {Position = UDim2.new(0, 3, 0.5, -8)}):Play()
        end
    end
    Refresh()

    Btn.MouseButton1Click:Connect(function()
        enabled = not enabled
        Refresh()
        callback(enabled)
    end)
end

local function AddSlider(name, min, max, default, callback)
    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(1, -4, 0, 50)
    Frame.BackgroundColor3 = Color3.fromRGB(25, 25, 40)
    Frame.BorderSizePixel = 0
    Frame.Parent = Container
    Instance.new("UICorner", Frame).CornerRadius = UDim.new(0, 7)

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(1, -10, 0, 20)
    Label.Position = UDim2.new(0, 10, 0, 3)
    Label.BackgroundTransparency = 1
    Label.Text = name .. ": " .. tostring(default)
    Label.TextColor3 = Color3.fromRGB(220, 220, 230)
    Label.TextSize = 12
    Label.Font = Enum.Font.Gotham
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Frame

    local Bar = Instance.new("Frame")
    Bar.Size = UDim2.new(1, -20, 0, 6)
    Bar.Position = UDim2.new(0, 10, 0, 32)
    Bar.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
    Bar.BorderSizePixel = 0
    Bar.Parent = Frame
    Instance.new("UICorner", Bar).CornerRadius = UDim.new(1, 0)

    local Fill = Instance.new("Frame")
    Fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    Fill.BackgroundColor3 = Color3.fromRGB(90, 45, 180)
    Fill.BorderSizePixel = 0
    Fill.Parent = Bar
    Instance.new("UICorner", Fill).CornerRadius = UDim.new(1, 0)

    local HitArea = Instance.new("TextButton")
    HitArea.Size = UDim2.new(1, 0, 0, 26)
    HitArea.Position = UDim2.new(0, 0, 0, 22)
    HitArea.BackgroundTransparency = 1
    HitArea.Text = ""
    HitArea.Parent = Frame

    local dragging = false
    HitArea.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
        end
    end)
    HitArea.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
            local rel = math.clamp((input.Position.X - Bar.AbsolutePosition.X) / Bar.AbsoluteSize.X, 0, 1)
            local val = math.floor(min + (max - min) * rel + 0.5)
            Fill.Size = UDim2.new(rel, 0, 1, 0)
            Label.Text = name .. ": " .. tostring(val)
            callback(val)
        end
    end)
end

-- ═══════════════════════════════════════
-- Создаём элементы GUI
-- ═══════════════════════════════════════
AddSeparator("MAIN")

AddToggle("Auto Parry", Config.Enabled, function(v)
    Config.Enabled = v
end)

AddToggle("Ping Based", Config.PingBased, function(v)
    Config.PingBased = v
end)

AddSeparator("DISTANCES")

AddSlider("Emergency Radius", 3, 25, Config.EmergencyRadius, function(v)
    Config.EmergencyRadius = v
end)

AddSlider("Clash Distance", 10, 50, Config.ClashDistance, function(v)
    Config.ClashDistance = v
end)

AddSeparator("TIMING")

AddSlider("Base Window", 20, 80, math.floor(Config.BaseWindow * 100), function(v)
    Config.BaseWindow = v / 100
end)

AddSlider("Min Window", 10, 50, math.floor(Config.MinWindow * 100), function(v)
    Config.MinWindow = v / 100
end)

AddSlider("Max Window", 40, 100, math.floor(Config.MaxWindow * 100), function(v)
    Config.MaxWindow = v / 100
end)

AddSlider("Ping Offset", 10, 30, math.floor(Config.PingBasedOffset * 10), function(v)
    Config.PingBasedOffset = v / 10
end)

AddSeparator("VISUALS")

AddToggle("Ball ESP", Config.BallESP, function(v)
    Config.BallESP = v
end)

AddToggle("Player ESP", Config.ShowESP, function(v)
    Config.ShowESP = v
end)

-- ═══════════════════════════════════════
-- РАБОЧАЯ ЛОГИКА ПАРИРОВАНИЯ
-- ═══════════════════════════════════════
local function FindBlockButton()
    local playerGui = LocalPlayer:FindFirstChild("PlayerGui")
    if not playerGui then return nil end

    local hotbar = playerGui:FindFirstChild("Hotbar")
    if hotbar then
        local block = hotbar:FindFirstChild("Block")
        if block and block:IsA("GuiButton") then
            return block
        end
    end

    for _, child in ipairs(playerGui:GetDescendants()) do
        if child:IsA("GuiButton") then
            if child.Name == "Block" or child.Text == "Block" then
                return child
            end
        end
    end
    return nil
end

local function ClickBlock()
    local btn = FindBlockButton()
    if not btn then return false end

    local clicked = false

    if getconnections then
        local function fireEvent(event)
            local conns = getconnections(event)
            if conns and #conns > 0 then
                for _, c in ipairs(conns) do
                    c:Fire()
                end
                return true
            end
            return false
        end
        if fireEvent(btn.Activated) then clicked = true end
        if fireEvent(btn.MouseButton1Click) then clicked = true end
        if fireEvent(btn.MouseButton1Down) then clicked = true end
    end

    if not clicked then
        pcall(function()
            btn.MouseButton1Click:Fire()
            clicked = true
        end)
    end

    return clicked
end

local function GetBall()
    local ballsFolder = Workspace:FindFirstChild("Balls")
    if not ballsFolder then return nil end
    local children = ballsFolder:GetChildren()
    for i = 1, #children do
        local v = children[i]
        if v:GetAttribute("realBall") == true then
            return v
        end
    end
    for i = 1, #children do
        local v = children[i]
        if v:IsA("BasePart") and v.Velocity.Magnitude > 15 then
            return v
        end
    end
    return nil
end

local function IsTargeted()
    if not LocalPlayer.Character then return false end
    return LocalPlayer.Character:GetAttribute("Targeted") == true
        or LocalPlayer.Character:FindFirstChild("Highlight") ~= nil
end

local function SendAction(isClashing, ball)
    if IsParrying then return false end
    local now = tick()
    local cd = isClashing and Config.ClashSpeed or Config.NormalSpeed

    if not isClashing and LastBallHit == ball and (now - LastClick) < 0.5 then return false end
    if (now - LastClick) < cd then return false end

    IsParrying = true
    LastClick = now
    LastBallHit = ball

    ClickBlock()

    task.delay(cd, function()
        IsParrying = false
    end)
    return true
end

local function UpdateVelocitySamples(speed)
    local now = tick()
    table.insert(velocitySamples, {time = now, speed = speed})
    if #velocitySamples > 15 then
        table.remove(velocitySamples, 1)
    end
    lastSampleTime = now
end

local function GetAverageSpeed()
    if #velocitySamples == 0 then return 0 end
    local sum = 0
    for _, sample in ipairs(velocitySamples) do
        sum = sum + sample.speed
    end
    return sum / #velocitySamples
end

local function GetAcceleration()
    if #velocitySamples < 3 then return 0 end
    local first = velocitySamples[1]
    local last = velocitySamples[#velocitySamples]
    local dt = last.time - first.time
    if dt <= 0 then return 0 end
    return (last.speed - first.speed) / dt
end

local function ComputeThreat(ball, root)
    if not ball or not root then return nil end
    local ballPos = ball.Position
    local playerPos = root.Position
    local distance = (playerPos - ballPos).Magnitude
    local velocity = ball.Velocity
    local speed = velocity.Magnitude
    if speed <= 0 then return nil end

    local directionToPlayer = (playerPos - ballPos).Unit
    local velocityDirection = velocity.Unit
    local dot = directionToPlayer:Dot(velocityDirection)

    return {
        ball = ball,
        distance = distance,
        speed = speed,
        dot = dot,
        timeToReach = distance / speed,
        velocity = velocity
    }
end

local function AutoParryLogic()
    if not Config.Enabled then return end
    if not LocalPlayer.Character then return end
    if not IsTargeted() then return end

    local ball = GetBall()
    if not ball then return end

    local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end

    local threat = ComputeThreat(ball, root)
    if not threat then return end

    UpdateVelocitySamples(threat.speed)

    local isClashing = threat.distance <= Config.ClashDistance

    if threat.distance <= Config.EmergencyRadius then
        SendAction(isClashing, ball)
        return
    end

    if threat.dot < 0.5 then return end

    local timeToReach = threat.timeToReach
    if Config.PingBased then
        local ping = 0
        local netStats = Stats:FindFirstChild("Network")
        if netStats then
            local serverStats = netStats:FindFirstChild("ServerStatsItem")
            if serverStats then
                local dataPing = serverStats:FindFirstChild("Data Ping")
                if dataPing and dataPing:IsA("NumberValue") then
                    ping = dataPing.Value / 1000
                end
            end
        end
        timeToReach = timeToReach - ping * Config.PingBasedOffset
        if timeToReach < 0 then timeToReach = 0 end
    end

    local avgSpeed = GetAverageSpeed()
    local accel = GetAcceleration()

    local window = Config.BaseWindow + avgSpeed * Config.SpeedWindowFactor + math.abs(accel) * Config.AccelWindowFactor
    window = math.clamp(window, Config.MinWindow, Config.MaxWindow)

    if timeToReach <= window then
        SendAction(isClashing, ball)
    end
end

-- ═══════════════════════════════════════
-- BALL ESP
-- ═══════════════════════════════════════
local ballHighlight = nil
local ballBB = nil

local function UpdateBallESP()
    local ball = GetBall()

    if not Config.BallESP or not ball then
        if ballHighlight then ballHighlight:Destroy() ballHighlight = nil end
        if ballBB then ballBB:Destroy() ballBB = nil end
        return
    end

    -- Highlight
    if not ballHighlight or ballHighlight.Parent ~= ball then
        if ballHighlight then ballHighlight:Destroy() end
        ballHighlight = Instance.new("Highlight")
        ballHighlight.FillColor = Color3.fromRGB(255, 40, 40)
        ballHighlight.OutlineColor = Color3.fromRGB(255, 255, 0)
        ballHighlight.FillTransparency = 0.3
        ballHighlight.Parent = ball
    end

    -- Дистанция
    if not ballBB or ballBB.Parent ~= ball then
        if ballBB then ballBB:Destroy() end
        ballBB = Instance.new("BillboardGui")
        ballBB.Size = UDim2.new(0, 100, 0, 26)
        ballBB.StudsOffset = Vector3.new(0, 3, 0)
        ballBB.AlwaysOnTop = true
        ballBB.Parent = ball

        local lbl = Instance.new("TextLabel")
        lbl.Name = "Dist"
        lbl.Size = UDim2.new(1, 0, 1, 0)
        lbl.BackgroundTransparency = 1
        lbl.TextColor3 = Color3.new(1, 1, 0)
        lbl.TextStrokeTransparency = 0.4
        lbl.TextSize = 14
        lbl.Font = Enum.Font.GothamBold
        lbl.Parent = ballBB
    end

    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local lbl = ballBB:FindFirstChild("Dist")
    if root and lbl then
        local dist = (ball.Position - root.Position).Magnitude
        lbl.Text = string.format("%.0f st", dist)
        if dist < Config.EmergencyRadius then
            lbl.TextColor3 = Color3.fromRGB(255, 50, 50)
        elseif dist < Config.ClashDistance then
            lbl.TextColor3 = Color3.fromRGB(255, 200, 0)
        else
            lbl.TextColor3 = Color3.fromRGB(100, 255, 100)
        end
    end
end

-- ═══════════════════════════════════════
-- PLAYER ESP
-- ═══════════════════════════════════════
local espCache = {}

local function UpdatePlayerESP()
    if not Config.ShowESP then
        for p, h in pairs(espCache) do
            if h then h:Destroy() end
            espCache[p] = nil
        end
        return
    end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            if not espCache[player] or espCache[player].Parent ~= player.Character then
                if espCache[player] then espCache[player]:Destroy() end
                local hl = Instance.new("Highlight")
                hl.FillColor = Color3.fromRGB(0, 130, 255)
                hl.OutlineColor = Color3.new(1, 1, 1)
                hl.FillTransparency = 0.6
                hl.Parent = player.Character
                espCache[player] = hl
            end
        end
    end
end

Players.PlayerRemoving:Connect(function(p)
    if espCache[p] then
        espCache[p]:Destroy()
        espCache[p] = nil
    end
end)

-- ═══════════════════════════════════════
-- Статус-бар
-- ═══════════════════════════════════════
local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(0, 180, 0, 24)
StatusLabel.Position = UDim2.new(0.5, -90, 1, -30)
StatusLabel.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
StatusLabel.BackgroundTransparency = 0.3
StatusLabel.BorderSizePixel = 0
StatusLabel.TextColor3 = Color3.fromRGB(80, 220, 80)
StatusLabel.TextSize = 11
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.Text = "⚔️ Hub Active"
StatusLabel.Parent = ScreenGui
Instance.new("UICorner", StatusLabel).CornerRadius = UDim.new(0, 6)

-- ═══════════════════════════════════════
-- ГЛАВНЫЙ ЦИКЛ
-- ═══════════════════════════════════════
RunService.Heartbeat:Connect(function()
    pcall(AutoParryLogic)
    pcall(UpdateBallESP)
    pcall(UpdatePlayerESP)

    -- обновляем статус
    if Config.Enabled then
        StatusLabel.Text = IsTargeted() and "⚔️ TARGETED — Parry Ready" or "⚔️ Hub Active"
        StatusLabel.TextColor3 = IsTargeted() and Color3.fromRGB(255, 80, 80) or Color3.fromRGB(80, 220, 80)
    else
        StatusLabel.Text = "⚔️ Hub Paused"
        StatusLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
    end
end)

-- ═══════════════════════════════════════
-- ЗАПУСК
-- ═══════════════════════════════════════
ScreenGui.Parent = game:GetService("CoreGui")

pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "Blade Ball Hub",
        Text = "Загружено! Auto Parry ON ⚔️",
        Duration = 4
    })
end)

print("[BB Hub] Loaded — Auto Parry | Ball ESP | Player ESP")
